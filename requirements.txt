# Runtime dependencies
# Note: sqlalchemy is required at runtime (used by src/alert_history.py and src/auth.py via ORM).
# It is managed via pyproject.toml (Poetry). If installing with pip, use requirements-dev.txt
# or install sqlalchemy separately: pip install sqlalchemy>=2.0
pylibrelinkup==0.10.0
PyYAML==6.0.1
requests==2.32.3
fastapi==0.115.12
uvicorn[standard]==0.34.0
cryptography==46.0.5